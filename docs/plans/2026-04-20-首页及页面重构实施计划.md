# 宝宝辅食小程序页面重构实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan with parallel agents.

**Goal:** 对小程序首页、食谱详情、档案页、历史页进行整体重构，采用温暖亲子风格

**Architecture:**
- 保持现有数据流不变（UniApp + 云函数）
- 重构重点：页面布局、信息架构、视觉样式
- 使用 Vue3 Composition API + TypeScript

**Tech Stack:** UniApp (Vue3 + TypeScript)

---

## 实施前备份

### Task 1: 备份现有代码

**Files:**
- 操作: 复制 src 目录到 `backup/src-bak-20260420`

**Step 1: 创建备份目录**
```bash
mkdir -p backup/src-bak-20260420
cp -r src/* backup/src-bak-20260420/
```

**Step 2: 验证备份**
```bash
ls -la backup/src-bak-20260420/
```
Expected: 显示 src 目录内容

---

## 全局样式定义

### Task 2: 定义全局样式变量

**Files:**
- Modify: `src/App.vue` 或创建 `src/styles/variables.scss`

**Step 1: 添加全局颜色变量**
```scss
// 品牌色
$primary: #FF9B5E;        // 暖橙
$primary-light: #FFE4D4;   // 浅橙
$background: #FFF8F3;      // 米白
$card-bg: #FFFFFF;         // 卡片白
$text-primary: #333333;    // 主文字
$text-secondary: #666666;  // 次级文字
$text-hint: #999999;       // 提示文字

// 餐次色块
$meal-breakfast: #FFE4D4;  // 早餐-浅橙
$meal-lunch: #E8F5E9;      // 午餐-浅绿
$meal-snack: #F3E5F5;      // 下午茶-浅紫
$meal-dinner: #E3F2FD;     // 晚餐-浅蓝

// 状态色
$status-pending: #FF9800;  // 待反馈-橙
$status-adopted: #4CAF50;  // 已采纳-绿
$status-rejected: #999999;  // 未采纳-灰
```

---

## 首页重构

### Task 3: 重构首页 index.vue

**Files:**
- Modify: `src/pages/index/index.vue`

**Step 1: 实现首页结构**
- 顶部：宝宝头像 + 昵称 + 月龄 + 日期
- 有食谱时：今日概要卡片
- 无食谱时：生成按钮
- 底部快捷入口

**Step 2: 添加温暖风格样式**
```scss
.container {
  min-height: 100vh;
  background: $background;
  padding: 24rpx;
}

.header-card {
  background: $card-bg;
  border-radius: 20rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.05);
}

.avatar {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  background: $primary-light;
}
```

---

## 食谱详情重构

### Task 4: 重构食谱详情页 recipe.vue

**Files:**
- Modify: `src/pages/recipe/recipe.vue`
- Modify: `src/pages/recipe/components/MealDetail.vue`

**Step 1: 实现四餐分块布局**
```vue
<view class="meal-card" v-for="meal in meals" :key="meal.type">
  <view class="meal-header">
    <view class="meal-type-badge" :class="meal.type">{{ meal.type }}</view>
    <text class="meal-time">{{ meal.duration }}分钟</text>
  </view>
  <view class="meal-name">{{ meal.name }}</view>
  <view class="meal-ingredients">
    <text class="ingredient-tag" v-for="ing in meal.ingredients" :key="ing">{{ ing }}</text>
  </view>
</view>
```

**Step 2: 添加餐次色块样式**
```scss
.meal-type-badge {
  padding: 8rpx 20rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
}
.meal-type-badge.breakfast { background: $meal-breakfast; color: #E65100; }
.meal-type-badge.lunch { background: $meal-lunch; color: #2E7D32; }
.meal-type-badge.snack { background: $meal-snack; color: #7B1FA2; }
.meal-type-badge.dinner { background: $meal-dinner; color: #1565C0; }
```

---

## 档案页重构

### Task 5: 重构档案页 profile.vue

**Files:**
- Modify: `src/pages/profile/profile.vue`

**Step 1: 实现分组布局**
```vue
<view class="form-section">
  <view class="section-title">基本信息</view>
  <!-- 昵称、出生日期、性别 -->
</view>

<view class="form-section">
  <view class="section-title">健康信息</view>
  <!-- 过敏原、口味偏好、不喜欢 -->
</view>

<view class="form-section">
  <view class="section-title">地区信息</view>
  <!-- 城市 -->
</view>
```

**Step 2: 添加分组样式**
```scss
.form-section {
  background: $card-bg;
  border-radius: 16rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
}

.section-title {
  font-size: 24rpx;
  color: $text-hint;
  margin-bottom: 24rpx;
  padding-bottom: 16rpx;
  border-bottom: 1rpx solid #f0f0f0;
}
```

---

## 历史页重构

### Task 6: 重构历史页 history.vue

**Files:**
- Modify: `src/pages/history/history.vue`

**Step 1: 实现卡片列表布局**
```vue
<view class="history-group" v-for="(items, date) in groupedHistory" :key="date">
  <view class="group-date-label">{{ formatDate(date) }}</view>
  <view class="history-card" v-for="item in items" :key="item._id">
    <view class="card-header">
      <text class="meal-type">{{ item.meals?.[0]?.type }}</text>
      <text class="meal-name">{{ item.meals?.[0]?.name }}</text>
    </view>
    <view class="card-status" :class="item.status">{{ getStatusText(item.status) }}</view>
  </view>
</view>
```

**Step 2: 添加卡片样式**
```scss
.history-card {
  background: $card-bg;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
}

.card-status.pending { color: $status-pending; background: #FFF3E0; }
.card-status.adopted { color: $status-adopted; background: #E8F5E9; }
```

---

## 实施后验证

### Task 7: 整体验证

**Step 1: 检查所有页面**
- 运行 UniApp 开发服务器
- 依次检查：首页、食谱详情、档案页、历史页

**Step 2: 验证交互流程**
- 无档案时点击生成 → 试用弹窗
- 有档案时点击生成 → 跳转详情页
- 查看历史 → 复用食谱

---

## Agent 协作分工

| Agent | 职责 |
|-------|------|
| 开发 Agent | 负责具体代码编写和实现 |
| Review Agent | 检查实施结果是否符合设计预期 |
| QA Agent | 检查代码质量（可读性、一致性、潜在问题） |

---

## 风险与注意事项

1. 备份现有代码后再开始重构
2. 保持现有云函数调用和数据流不变
3. 确保所有页面在不同手机尺寸下显示正常
4. 保持兼容性：iOS 和 Android
